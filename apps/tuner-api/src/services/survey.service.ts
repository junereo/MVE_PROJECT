import { PrismaClient } from '@prisma/client';

const prisma = new PrismaClient();

// TODO: Implement survey service methods
export const createSurveyService = async (data: any) => {
    // Implementation will be added later
};

export const getSurveyService = async (id: number) => {
    // Implementation will be added later
}; 